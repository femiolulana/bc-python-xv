def even_numbers(low, high):
        for i in range(low, high): #looping through the given nos.
                if i % 2 == 0:
                        print i    #printing one per line
		
even_numbers(2,40)	#quick testing,delete when to be applied to an app
